include:
  - docker-compose.yml

services:
  gateway:
    build:
      context: ../services
      dockerfile: gateway/Dockerfile
    container_name: browser-agent-gateway
    ports: ["8000:8000"]
    environment:
      CORS_ORIGINS: ${CORS_ORIGINS:-*}
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD:-password}@postgres:5432/browser_agent
      ORCHESTRATOR_URL: http://orchestrator:8001
      KEYCLOAK_REALM_URL: http://keycloak:8080/realms/browser-agent
      KEYCLOAK_AUDIENCE: browser-agent-extension
      BROWSER_TOOL_TIMEOUT: "60"
    depends_on:
      postgres:
        condition: service_healthy
      keycloak:
        condition: service_healthy

  orchestrator:
    build:
      context: ../services
      dockerfile: ochestrator/Dockerfile
    container_name: browser-agent-orchestrator
    expose: ["8001"]
    environment:
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      ORCHESTRATOR_MODEL: llama3.1:8b
      CHAT_AGENT_URL: http://chat-agent:8002
      BROWSER_AGENT_URL: http://browser-agent:8003
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD:-password}@postgres:5432/browser_agent
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy

  chat-agent:
    build:
      context: ../services
      dockerfile: chat_agent/Dockerfile
    container_name: browser-agent-chat-agent
    expose: ["8002"]
    environment:
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      CHAT_MODEL: qwen2.5:7b
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD:-password}@postgres:5432/browser_agent
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy

  browser-agent:
    build:
      context: ../services
      dockerfile: browser_agent/Dockerfile
    container_name: browser-agent-browser-agent
    expose: ["8003"]
    environment:
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      BROWSER_MODEL: qwen2.5:14b
      GATEWAY_URL: http://gateway:8000
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD:-password}@postgres:5432/browser_agent
      BROWSER_TOOL_TIMEOUT: "65"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
      gateway:
        condition: service_started
