[project]
name = "browser-relay"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.0.0",
    "redis[hiredis]>=5.0.0",
    "pydantic-settings>=2.0.0",
    "uvicorn[standard]>=0.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
