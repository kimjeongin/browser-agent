{{- if and .Values.chatAgent.enabled .Values.chatAgent.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: chat-agent
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "browser-agent.componentLabels" (dict "component" "chat-agent" "context" .) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: chat-agent
  minReplicas: {{ .Values.chatAgent.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.chatAgent.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.chatAgent.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
