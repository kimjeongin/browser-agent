{{- /*
  Placeholder Secret for PostgreSQL.  In production replace this with
  External Secrets Operator or Sealed Secrets.  The Secret is only
  created if it does not already exist (use --set to provide values).
  We do NOT render passwords in plain text here; users must pre-create
  the Secret before installing or supply via ESO/Vault.

  This file is intentionally left empty — the Secret is documented in
  NOTES.txt and must be created manually or by a secrets operator.
*/ -}}
